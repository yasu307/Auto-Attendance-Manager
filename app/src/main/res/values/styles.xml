<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:fontFamily">@font/notosansjp_regular</item>
    </style>

    <style name="TimePickerDialogTheme" parent="Theme.AppCompat.Light.Dialog">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:timePickerStyle">@style/TimePicker</item>
    </style>

    <style name="TimePicker"
        parent="@android:style/Widget.Material.Light.TimePicker">
        <item name="android:timePickerMode">spinner</item>
    </style>

    <attr name="customAttr" format="reference"/>
    <style name="TextViewStroke">
        <item name="android:textColor">@color/BLACK</item>
        <item name="android:gravity">center</item>
        <item name="android:background">@drawable/textview_drawable</item>
        <item name="android:autoSizeTextType">uniform</item>
    </style>

    <style name="TextView">
        <item name="android:textColor">@color/BLACK</item>
        <item name="android:gravity">center</item>
        <item name="android:background">@color/WHITE</item>
        <item name="android:autoSizeTextType">uniform</item>
    </style>

    <style name="TextViewTrans">
        <item name="android:textColor">@color/BLACK</item>
        <item name="android:gravity">center</item>
        <item name="android:background">@color/TRANS</item>
        <item name="android:autoSizeTextType">uniform</item>
    </style>

</resources>
